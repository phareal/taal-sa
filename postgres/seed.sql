-- ─────────────────────────────────────────────────────────────────
-- postgres/seed.sql — Snapshot complet : schéma + données initiales
-- Généré par : make snapshot
-- Chargé automatiquement au premier démarrage Docker (init script 02)
--
-- Pour regénérer depuis la DB courante :   make snapshot
-- Pour repartir de zéro avec ces données : make bootstrap
-- ─────────────────────────────────────────────────────────────────

-- (fichier vide — exécuter `make snapshot` pour le remplir)
